<div class="conainer">
  <h2>固定費科目編集</h2>
  <%= form_with model: @fixedcost, method: :put, local: true do |form| %>
  <%= render "fixedcosts/form", form: form %>
  <div><%= form.submit "更新" %></div>
  <% end %>
</div