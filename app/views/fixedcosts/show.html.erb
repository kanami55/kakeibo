<%= link_to "編集", edit_fixedcost_path(@fixedcost) %>
<%= link_to "固定費科目一覧へ戻る", fixedcosts_path %>
<table>
  <tr>
    <th>名称</th>
    <td><%= @fixedcost.name %></td>
  </tr>
  <tr>
    <th>詳細</th>
    <td><%= @fixedcost.description %></td>
  </tr>
</table>